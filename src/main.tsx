import React from 'react';
import ReactDOM from 'react-dom/client';
import TBSBankSoal from './TBSBankSoal';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TBSBankSoal />
  </React.StrictMode>,
);
